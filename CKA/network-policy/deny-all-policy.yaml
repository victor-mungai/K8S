apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: default-deny-all-ingress
spec:
  podSelector: {} # Selects ALL pods in the namespace
  policyTypes:
    - Ingress
# No ingress rules specified, meaning nothing is allowed by default

