apiVersion: v1
kind: Pod
metadata:
  name: pod
spec:
  containers:
    - name: app
      image: busybox
      command: ["sh","-c"]
      args: ["sleep 1h"]
      securityContext:
        runAsUser: 0
