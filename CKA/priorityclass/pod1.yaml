apiVersion: v1
kind: Pod
metadata:
  name: low-priority-pod
kind: Pod
spec:
  containers:
    - name: nginx
      image: nginx
      resources:
        requests:
          memory: 128Mi
          cpu: 1000m
  priorityClassName: low-priority

