apiVersion: external-secrets.io/v1beta1
kind: ClusterSecretStore
metadata:
  name: vault-backend
spec:
  provider:
    vault:
      server: "http://192.168.56.10:8200"
      path: "kv/org"
      version: "v2"
      auth:
        tokenSecretRef:
          name: "vault-token"
          namespace: "demo"
          key: "token"
